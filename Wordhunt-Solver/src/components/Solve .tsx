import React from "react";

export default function Solve(props) {
	const { propsGrid } = props;
	const wordList = new Array<string>();
	const grid = new Array<Array<string>>(4);

	console.log(grid);

	return <div>Solve </div>;
}
